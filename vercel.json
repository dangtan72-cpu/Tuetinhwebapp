{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma migrate deploy && npx tsx scripts/update-contact.ts && next build",
  "installCommand": "npm install"
}
